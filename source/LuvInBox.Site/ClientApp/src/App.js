import * as React from 'react';
import Routes from './routes';
import Layout from './components/layout';

import './custom.css';

export default () => (
    <Layout>
        <Routes />
    </Layout>
);
